#include <bits/stdc++.h>
using namespace std;

int main()
{
    // // o(1);
    // vector<int> x = {10, 20, 30};
    // vector<int> v = {1, 2, 3};
    // //  v = x;
    // for (int i = 0; i < v.size(); i++)
    // {
    //     cout << v[i] << " ";
    // }

    // o(n);
    // vector<int> x = {10, 20, 30, 40};
    // vector<int> v = {1, 2, 3};
    // v = x;
    // for (int i = 0; i < v.size(); i++)
    // {
    //     cout << v[i] << " ";
    // }

    // vector<int> v = {10, 20, 30, 40};
    // v.push_back(50);
    // for (int i = 0; i < v.size(); i++)
    // {
    //     cout << v[i] << " ";
    // }

    // vector<int> v = {10, 20, 30, 40};
    // v.pop_back();
    // for (int i = 0; i < v.size(); i++)
    // {
    //     cout << v[i] << " ";
    // }

    // vector<int> v = {10, 20, 30, 40};
    // // v.insert(v.begin()+2, 10); // This is use for single value insert
    //  vector<int> v2 = {101, 201, 301, 401};
    // v.insert(v.begin()+2, v2.begin(), v2.end()); // This is use for multiple value insert
    // for (int x: v)
    // {
    //     cout << x << " ";
    // }

    // vector<int> v = {10, 20, 30, 40, 50};
    // // v.erase(v.begin()+2); // This is use for single value delete
    // v.erase(v.begin() + 1, v.end() - 2); // This is use for multiple value delete
    // for (int x : v)
    // {
    //     cout << x << " ";
    // }

    return 0;
}